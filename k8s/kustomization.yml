apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  namespace: glenneligio
configMapGenerator:
  - name: dntx-config
    namespace: glenneligio
    files:
      - config.yml
resources:
  - database.yml
  - frontend.yml
  - backend.yml
